#include <stdio.h>

int main (void){
    int number;
    int i;

    prirntf("値：");
    scanf("%d",&number);

    for ( i = number; i < i; i++)
    {
        for ( i = 0; i < count; i++)
        {

        }
        
        printf("%d",i);
    }
    

    return 0;
}